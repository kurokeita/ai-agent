# Suggested Commands

Since this project is a static collection of skills, there are no build or run commands.

## Utility Commands

- **List Skills**: `ls -R skills`
- **Search Content**: `grep -r "pattern" skills/`
- **Lint Markdown**: `npx markdownlint-cli "skills/**/*.md"` (Recommended if Node.js is available)
